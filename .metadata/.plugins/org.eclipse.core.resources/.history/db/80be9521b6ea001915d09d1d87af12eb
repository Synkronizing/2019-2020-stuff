public class ReverseNumber {
	public ReverseNumber(int num_send) {
		int rev=0;
		int b=Integer.toString(num_send).length();
		while(b>0) {
			int send = num_send % 10;
			rev = (rev*10)+send;
		}
		
	}

}
